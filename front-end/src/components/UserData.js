import React from "react";
import CardComponent from "./CardComponent";

const UserData = ({ user, userInitials }) => {
  return (
    <CardComponent>
      {/* Your Avatar and Typography code here */}
    </CardComponent>
  );
};

export default UserData;
